---
name: relay-exercise-run
description: 'Execute realistic scenarios against a capability (or group) within the active session. In goal-mode sessions, walks the journey end-to-end with adaptive gap handling (alternative / file / skip). Writes to .relay/exercise/<session>/<capability>.md (default) or step-<N>-<capability>.md (goal). Use after /relay-exercise creates a session.'
---

Follow the instructions in ./workflow.md.
